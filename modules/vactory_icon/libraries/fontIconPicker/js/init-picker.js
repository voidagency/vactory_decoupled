jQuery(document).ready(function ($) {
    $('#vactory_icon_picker').fontIconPicker(
        {
            // emptyIcon: false,
            // emptyIconValue: 'none'
        }
    ); // Load with default options
});
