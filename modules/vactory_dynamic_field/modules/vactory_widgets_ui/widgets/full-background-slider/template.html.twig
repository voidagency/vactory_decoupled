<div>
  {% if extra_fields.bigTitle is not empty %}
    <h2>{{ extra_fields.bigTitle }}</h2>
  {% endif %}
  {% if extra_fields.intro is not empty %}
    <p>{{ extra_fields.intro }}</p>
  {% endif %}

  <div>
    {% for item in content %}
      <div>
        {% if item.image_alt is not empty %}
          <p>{{ item.image_alt }}</p>
        {% endif %}
        {% if item.title is not empty %}
          <h3>{{ item.title }}</h3>
        {% endif %}
        <div>
          {{ item.description.value|raw }}
        </div>
      </div>
      {% if item.videoId is not empty %}
        <p>{{ "https://www.youtube.com/watch?v=" ~ item.videoId }}</p>
      {% endif %}
      {% if item.link.title is not empty and item.link.url is not empty %}
        <a href="{{ item.link.url }}">{{ item.link.title }}</a>
      {% endif %}
    {% endfor %}
  </div>
</div>
